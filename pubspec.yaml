name: journal_app
description: Journal application project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  http: ^0.13.4
  flutter_bloc: ^7.3.0
  equatable: ^2.0.3
  easy_localization: ^3.0.0
  url_launcher: ^6.0.12
  graphql_flutter: ^5.0.1
  freezed: ^1.1.1
  freezed_annotation: ^1.1.0
  json_serializable: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.7
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/languages/